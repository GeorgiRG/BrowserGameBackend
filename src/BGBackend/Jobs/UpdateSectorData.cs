using BrowserGameBackend.Data;
using BrowserGameBackend.Tools.GameTools;
using Quartz;

namespace BrowserGameBackend.Jobs
{
    /*
    public class UpdateSectorData : IJob
    {
        private readonly GameContext _context;
        private readonly int _sectorAmount = GeneratorTool.galaxyWidth;
        public UpdateSectorData(GameContext context)
        {
            _context = context;
        }

        public Task Execute()
        {
            
            return Task.CompletedTask;

        }
    }*/
}
